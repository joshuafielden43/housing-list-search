# Optional OCR/layout fallback for hard PDFs (scanned flyers, scrambled text order).
# Install: uv pip install -r requirements-ocr.txt --python .venv/bin/python
# Disable at runtime: HLS_DISABLE_MARKER_PDF=1
-r requirements.txt
marker-pdf==1.10.2