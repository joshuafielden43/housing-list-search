# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/

# Node (Husky / lint-staged hook runner)
node_modules/

# Output artifacts — generated at runtime (not source code)
current_full.csv
diff.csv
run_prev.csv
daily_summary.md
daily_summary_partial.md
changelog_diffs.*
housing_registry.db
gilroy_*.csv
*_audit.csv
*.db

# IDE / editor / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Playwright browsers (large, installed separately)
ms-playwright/

# Any local scratch or temp data
tmp/
temp/
*.tmp

# Generated by db management tooling
snapshots/

# Cron / run_daily.sh runtime artifacts
logs/
.run_daily.lock
.run_daily.lock.d

# Personal session breadcrumbs / private notes (never committed)
SESSION_*.md
BREADCRUMBS*.md
NOTES_*.md
private/
scratch/

# ===== git-hardening secrets block (managed default — keep in sync) =====
# Env files (templates stay tracked via the negations below)
.env
.env.*
*.env
!.env.example
!.env.sample
!.env.template
!.env.dist
# Auth / secret stores
auth.json
*.secret
*secrets.json
*secrets.yaml
*secrets.yml
# Private keys / certs
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
# Credentials
.netrc
.htpasswd
.pgpass
credentials
credentials.*
# ===== end git-hardening secrets block =====
